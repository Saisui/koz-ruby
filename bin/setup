#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -vx

bundle install

# Do any other automated setup that you need to do here

mkdir ~/.kozbot
cd ~/.kozbot
mkdir media_cache
mkdir chat
touch config.yaml
